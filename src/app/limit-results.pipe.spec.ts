import { LimitResultsPipe } from './limit-results.pipe';

describe('LimitResultsPipe', () => {
  it('create an instance', () => {
    const pipe = new LimitResultsPipe();
    expect(pipe).toBeTruthy();
  });
});
